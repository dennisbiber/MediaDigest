This directory is bind-mounted into the pipelines container at /app/pipelines.
`digest_pipeline.py` here is a deploy copy of ../../../../interfaces/owui/digest_pipeline.py
(the canonical source). If you change the canonical pipeline, copy it here too.
